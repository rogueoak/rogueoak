# Keep the SHA-pinned GitHub Actions in .github/workflows current. Dependabot
# bumps the pin and updates the trailing `# vX` comment, so we get security
# updates without trusting mutable tags at runtime.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
